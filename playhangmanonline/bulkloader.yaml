# Autogenerated bulkloader.yaml file.
# You must edit this file before using it. TODO: Remove this line when done.
# At a minimum address the items marked with TODO:
#  * Fill in connector and connector_options
#  * Review the property_map.
#    - Ensure the 'external_name' matches the name of your CSV column,
#      XML tag, etc.
#    - Check that __key__ property is what you want. Its value will become
#      the key name on import, and on export the value will be the Key
#      object.  If you would like automatic key generation on import and
#      omitting the key on export, you can remove the entire __key__
#      property from the property map.

# If you have module(s) with your model classes, add them here. Also
# change the kind properties to model_class.
python_preamble:
- import: base64
- import: re
- import: google.appengine.ext.bulkload.transform
- import: google.appengine.ext.bulkload.bulkloader_wizard
- import: google.appengine.ext.db
- import: google.appengine.api.datastore
- import: google.appengine.api.users

transformers:

- kind: Game
  connector: csv
  connector_options:
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: date
      external_name: date
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: finished
      external_name: finished
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: letters_tried
      external_name: letters_tried
      # Type: String Stats: 5 properties of this type in this kind.

    - property: player
      external_name: player
      # Type: User Stats: 15 properties of this type in this kind.
      import_transform: transform.none_if_empty(users.User)  # Assumes email address

    - property: right_letters
      external_name: right_letters
      # Type: String Stats: 15 properties of this type in this kind.

    - property: using_xmpp
      external_name: using_xmpp
      # Type: Boolean Stats: 15 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: word
      external_name: word
      import_transform: transform.create_foreign_key('Word')
      export_transform: transform.key_id_or_name_as_string

    - property: wrong_letters
      external_name: wrong_letters
      # Type: String Stats: 15 properties of this type in this kind.


- kind: UserStatistics
  connector: csv
  connector_options:
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: games_over_xmpp
      external_name: games_over_xmpp
      # Type: Integer Stats: 3 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: games_played
      external_name: games_played
      # Type: Integer Stats: 3 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: player
      external_name: player
      # Type: User Stats: 3 properties of this type in this kind.
      import_transform: transform.none_if_empty(users.User)  # Assumes email address

    - property: times_hanged
      external_name: times_hanged
      import_transform: transform.none_if_empty(int)


- kind: Word
  connector: csv
  connector_options:
  property_map:
    - property: __key__
      external_name: key
      import_transform: int
      export_transform: transform.key_id_or_name_as_string

    - property: hint
      external_name: hint

    - property: word
      external_name: word

